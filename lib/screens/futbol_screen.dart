import 'package:flutter/material.dart';

class FutbolScreen extends StatelessWidget {
   
  const FutbolScreen({Key? key}) : super(key: key);
  
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
         child: Text('FutbolScreen'),
      ),
    );
  }
}