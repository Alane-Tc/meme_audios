import 'package:flutter/material.dart';

class GamesAudiosScreen extends StatelessWidget {
   
  const GamesAudiosScreen({Key? key}) : super(key: key);
  
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
         child: Text('GamesAudiosScreen'),
      ),
    );
  }
}